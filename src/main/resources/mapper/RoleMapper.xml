<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.bupt.sse.nmp.dao.RoleMapper">
  <resultMap id="BaseResultMap" type="cn.bupt.sse.nmp.entity.Role">
    <constructor>
      <arg column="roleId" javaType="java.lang.Integer" jdbcType="INTEGER" />
      <arg column="roleName" javaType="java.lang.String" jdbcType="VARCHAR" />
      <arg column="description" javaType="java.lang.String" jdbcType="VARCHAR" />
      <arg column="type" javaType="java.lang.Integer" jdbcType="INTEGER" />
    </constructor>
  </resultMap>
  <insert id="insert" parameterType="cn.bupt.sse.nmp.entity.Role">
    insert into role (roleId, roleName, description,type
      )
    values (#{roleId,jdbcType=INTEGER}, #{roleName,jdbcType=VARCHAR}, #{description,jdbcType=VARCHAR}, #{type,jdbcType=INTEGER}
      )
  </insert>
  <insert id="insertSelective" parameterType="cn.bupt.sse.nmp.entity.Role">
    insert into role
    <trim prefix="(" suffix=")" suffixOverrides=",">
      <if test="roleId != null">
        roleId,
      </if>
      <if test="roleName != null">
        roleName,
      </if>
      <if test="description != null">
        description,
      </if>
    </trim>
    <trim prefix="values (" suffix=")" suffixOverrides=",">
      <if test="roleId != null">
        #{roleId,jdbcType=INTEGER},
      </if>
      <if test="roleName != null">
        #{roleName,jdbcType=VARCHAR},
      </if>
      <if test="description != null">
        #{description,jdbcType=VARCHAR},
      </if>
    </trim>
  </insert>

  <select id="selectByRoleId" parameterType="java.lang.Integer" resultType="Role">
    select * from role where roleId = #{roleId, jdbcType=INTEGER}
  </select>
  <update id="updateRole" parameterType="Role">
    update role set roleName = #{roleName}, description = #{description} where roleId = #{roleId}
  </update>
  <delete id="delRoleById" parameterType="java.lang.Integer">
    delete from role where roleId = #{roleId}
  </delete>
  <select id="selectAll" resultMap="BaseResultMap">
    select * from role
  </select>

  <select id="selectTypeByUserId" resultType="java.lang.Integer" parameterType="java.lang.Integer">
      select type from user, user_role, role where user .userId = user_role.userId and role.roleId = user_role.roleId and user.userId = #{roleId}
  </select>
</mapper>